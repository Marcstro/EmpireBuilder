package entities.units.AI;

// TODO this is a temporary AI class to make units decide their actions, it should be replaced by any real system
public enum Focus {

    RAIDING,
    DEFENDING_EXTERNAL_AREA,
    IDLING,
    IS_RETURNING_WITH_LOOT,
    RETURNING_TO_BASE
}
